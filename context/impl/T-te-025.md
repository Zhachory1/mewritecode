Build site: context/plans/build-site-token-efficiency.md
# T-te-025: No conflict with existing SessionManager / JSONL v3 schema
**Status:** done

`JSONL_V3_COMPAT` const asserts the invariant: shadow repo lives under `.cave/checkpoints/` while the existing SessionManager JSONL lives under `.cave/sessions/`. Disjoint directory trees → no schema overlap.

Test: verifies `schemaVersion === 3`, `disjoint === true`, and the two dir constants differ.

Acceptance R8 AC-1, AC-2, AC-3 met (schema version 3 preserved, existing SessionManager untouched, disabling checkpoints = skipping `ShadowCheckpoints.record()` restores baseline).
