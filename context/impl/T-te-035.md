Build site: context/plans/build-site-token-efficiency.md
# T-te-035: User pricing table override
**Status:** done

`PricingResolver.override(model, price)` mutates the in-memory table. Next `estimateDollars()` call uses the new rates.

Test: "override changes next estimate" asserts a 2x rate doubles the estimate.

Acceptance R1 AC-3 met.
