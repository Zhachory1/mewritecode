Build site: context/plans/build-site-token-efficiency.md
# T-te-132: SWE-bench Verified harness adapter
**Status:** done (adapter shape; real SWE-bench repos wired at runtime)
`packages/agent/src/bench/swe-bench.ts` — `runBench(instances, { perInstanceCapDollars, runInstance })` produces `BenchInstanceResult[]` with `{instance_id, resolved, attempts, dollars_spent, cost_cap_failure?, duration_ms, traces}`. The real runner fetches SWE-bench Verified instances and calls the cave agent per instance; the adapter contract is test-covered with a mock runner.
Tests: per-instance JSON shape, two-run determinism under stub, cap enforcement.
Acceptance R1 AC-1, AC-2, AC-3 met at adapter level.
