Build site: context/plans/build-site-token-efficiency.md
# T-te-018: Linux Landlock wrapper with kernel 5.13 detection
**Status:** done

`packages/agent/src/sandbox/landlock.ts`:
- `parseKernelVersion(release)` → `{major, minor}`
- `supportsLandlock(info)` → `true` when ≥5.13
- `detectLandlockSupport(platform, release)` one-shot cached detection
- `landlockSandbox()` returns permissive profile with reason when unsupported
- `__resetLandlockCache()` test-only reset

Tests: parse, support threshold, permissive fallback on old kernel, cached detection.
Acceptance R2 AC-1, AC-2, AC-3 met. Real landlock syscall wiring is the caller's responsibility at tool-dispatch time — wrap() emits a `CAVE_LANDLOCK_WORKDIR=...` env prefix marker the runner unwraps.
