Build site: context/plans/build-site-token-efficiency.md
# T-te-034: CostEntry struct + per-provider pricing table
**Status:** done

`packages/agent/src/cost/types.ts` — `CostEntry` shape: `turnIndex`, `model`, `provider`, input/cached/cacheWrite/output tokens, `dollarsEstimated`.

`packages/agent/src/cost/pricing.ts` — `PricingResolver`:
- `DEFAULT_PRICING` covers opus/sonnet/haiku/gpt-5 with input/cached/cacheWrite/output per-million rates
- `override(model, price)`, `bulkOverride()` for user config
- `priceOf()`, `estimateDollars()`

Tests: default table, dollar estimation from tokens, unknown-model zero fallback, DEFAULT_PRICING coverage.
Acceptance R1 AC-1, AC-2 met.
