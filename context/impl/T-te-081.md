Build site: context/plans/build-site-token-efficiency.md
# T-te-081: Download ONNX models on first use with checksum gate
**Status:** done

Implemented:
- `packages/agent/src/compression/model-download.ts` — `downloadModel()`, `verifyChecksum()`, `isModelCached()`, `LLMLINGUA2_MANIFEST`
- Model stored at `~/.cave/models/llmlingua2-bert-base.onnx`
- SHA256 checksum gate on download (placeholder hash until upstream pins artifact)
- Atomic download: writes `.tmp`, verifies, renames
- Progress callback for UI integration
- `LLMLinguaMiddleware.compressAsync()` — auto-downloads + loads ONNX session via lazy `import("onnxruntime-node")`
- `LLMLinguaMiddleware.initOnnx()` — explicit init for callers who need pre-warming
- Sync `compress()` preserved; throws if ONNX not pre-initialized
- `onnxruntime-node` is optional peer dep (lazy import, try/catch)
- Full ONNX inference pipeline (tokenizer → model → token selection) deferred to T-082; current path validates session loadability then routes through deterministic compressor
- 5 tests pass in model-download.test.ts
