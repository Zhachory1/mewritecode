Build site: context/plans/build-site-token-efficiency.md
# T-te-016: macOS Seatbelt sandbox profile
**Status:** done

`packages/agent/src/sandbox/seatbelt.ts` — `buildSeatbeltProfile()` emits a sandbox-exec `.sb` profile and `seatbeltSandbox()` wraps commands as `sandbox-exec -p '<profile>' sh -c '<cmd>'`.

Profile reloadable per call via the `allow` parameter. Real execution (`child_process.spawn` of `sandbox-exec`) is the caller's responsibility at tool-dispatch time.

Tests (sandbox.test.ts): profile contains `(deny default)`, workdir write allow, network deny default, sensitive home path deny. Acceptance R1 AC-1..AC-4 met at the profile-bytes level.
