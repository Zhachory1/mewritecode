Build site: context/plans/build-site-token-efficiency.md
# T-te-024: Auto-commit on mutating tool calls, tagged to session entry ID
**Status:** done

`ShadowCheckpoints.record(entryId, tool)`:
- Ignores non-mutating tools → returns `null`, no log entry
- On mutating tool: creates a `CheckpointEntry` with `sessionId`, `entryId`, `commitSha`, `timestamp`, `mutating: true`

Tests: write/edit/bash produce commits, read/grep/ls do not, count increments per mutating call.
Acceptance R1 AC-1, AC-2, AC-3 met.
