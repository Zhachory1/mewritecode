Build site: context/plans/build-site-token-efficiency.md
# T-te-133: Per-instance hard cost caps + cost_cap_failure logging
**Status:** done
`runBench` flags `cost_cap_failure: true` on any instance where `dollarsSpent > perInstanceCapDollars`. `aggregateBench()` reports `capFailures` count.
Test: "flags cost_cap_failure when instance exceeds cap", "aggregate ≤ cap × instance count".
Acceptance R2 AC-1, AC-2, AC-3 met.
