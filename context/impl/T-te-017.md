Build site: context/plans/build-site-token-efficiency.md
# T-te-017: Seatbelt blocks writes outside workdir, network, sensitive HOME
**Status:** done

Profile bytes assert (sandbox.test.ts):
- `(allow file-write* (subpath "<workdir>"))` — only workdir writable
- `(deny network*)` by default
- `(deny file-read*/write* (subpath "~/.ssh"))` — same for `.aws`, `.gnupg`, `.config/gcloud`, `.netrc`

Combined with deny-default base, writes outside workdir are blocked.

Acceptance R1 AC-1, AC-2, AC-3 met.
