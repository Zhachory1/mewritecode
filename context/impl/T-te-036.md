Build site: context/plans/build-site-token-efficiency.md
# T-te-036: Append-only trace JSONL at `~/.cave/sessions/<id>.trace.jsonl`
**Status:** done

`packages/agent/src/cost/trace-writer.ts`:
- `TraceSink` interface; `createInMemoryTraceSink(name, rotateBytes)` impl used by tests
- `TraceEmitter.emit(type, turn, payload)` — produces `TraceEvent` with monotonic `seq`
- Events carry `type` field (llm_call / tool_call / tool_cache_hit|miss / cost_cap_turn|session / compression_fallback / subagent_start|end)

Tests: one event per line, every event has `type`, monotonic `seq`, file never rewritten in place (snapshot preserves prior events).
Acceptance R5 AC-1, AC-2, AC-3 met. Real `fs.appendFileSync` wiring at runtime boundary; in-memory sink used for unit tests so the contract is byte-checkable without I/O.
