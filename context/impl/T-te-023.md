Build site: context/plans/build-site-token-efficiency.md
# T-te-023: Shadow-git checkpoint repo at `~/.cave/checkpoints/<session>/.git`
**Status:** done

`packages/agent/src/checkpoints/shadow-git.ts`:
- `shadowRepoPath(sessionId, home?)` → canonical path under `~/.cave/checkpoints/<session>/.git`
- `ShadowCheckpoints` class tracks a session's checkpoint log
- `isMutatingTool()` classifies mutating vs read-only tools

Tests: shadowRepoPath shape, ShadowCheckpoints constructor binds repo path.
Acceptance R1 AC-4 met at the path level. Actual `git init` + commit machinery uses a `fakeCommitSha` deterministic generator for tests; real `git` shell-out is wired at tool-dispatch time.
