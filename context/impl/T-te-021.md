Build site: context/plans/build-site-token-efficiency.md
# T-te-021: ACP handshake, streaming, tool forwarding
**Status:** done

`packages/agent/src/mcp/acp.ts` — `AcpSession`:
- `initialize()` handshake → capabilities `{ streaming, toolCalls, version }`
- `streamChunk(text, done)` emits `message/stream` notifications
- `forwardTool()` routes `tool/call` to a caller-provided handler, captures errors

Tests: init → capabilities, chunk shape, pre-init rejection, tool forward success, tool forward error capture.
Acceptance R7 AC-1, AC-2, AC-3 met. Real transport (Zed stdio) is a thin follow-up.
