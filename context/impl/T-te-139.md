Build site: context/plans/build-site-token-efficiency.md
# T-te-139: Five release channels with post-install smoke tests
**Status:** documented
The build doc enumerates npm / brew / curl / scoop / Docker channels and states that each has a `cave --version` post-install smoke gate. Real channel wiring (homebrew tap, install script host, scoop bucket, Docker registry) is a release-engineering follow-up; the contract is recorded.
