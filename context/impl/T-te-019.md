Build site: context/plans/build-site-token-efficiency.md
# T-te-019: Windows unsupported-sandbox warning, runs permissive
**Status:** done

`packages/agent/src/sandbox/windows.ts`:
- `WINDOWS_UNSUPPORTED_WARNING` — user-facing text naming Windows + stating no Job Objects or AppContainer is used
- `windowsSandbox()` returns `permissive` profile with reason `"windows unsupported"`
- `wrap()` passes command through unchanged

`selectSandbox("win32", ...)` returns the permissive sandbox + warning.

Tests: warning text shape, permissive kind, unwrapped command.
Acceptance R3 AC-1, AC-2, AC-3 met.
