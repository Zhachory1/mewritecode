Build site: context/plans/build-site-token-efficiency.md
# T-te-135: README SWE-bench badge updated from nightly job output
**Status:** partial (workflow commits README update; badge URL template ready)
The `bench-nightly.yml` workflow `git add`s the README alongside the new results file; a small post-processing step (in the workflow) regenerates the badge line from the latest nightly JSON. Badge URL template uses shields.io with a dynamic endpoint pointing at the most recent results file.
